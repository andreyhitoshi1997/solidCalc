package model;

public interface Operacoes <a, b> {
    int somar(Integer a, Integer b);

    int subtrair(Integer a, Integer b);

    int multiplicar(Integer a, Integer b);

    int dividir(Integer a, Integer b);
}
